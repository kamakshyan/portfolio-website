---
const response = await fetch("https://api.quotable.io/random?maxLength=50&tags=technology", {
	method: "GET",
    headers: {"Accept": "*/*"} 
}).then(res => res.json())

const quote = response.content;
const author = response.author;
---

<div class="fixed font-scp flex flex-col gap-2 bottom-0 p-10 md:px-64 text-sm text-white">
    <p>
        "{quote}"
    </p>
    <p class="inline-block ml-auto">
        ~ {author}
    </p>
</div>